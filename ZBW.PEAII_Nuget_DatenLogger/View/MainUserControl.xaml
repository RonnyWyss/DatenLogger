<UserControl x:Class="ZBW.PEAII_Nuget_DatenLogger.View.MainUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ZBW.PEAII_Nuget_DatenLogger.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- <local:DatenLoggerView x:Name="DatenLoggerView" ></local:DatenLoggerView> -->
        <!-- <local:DatenLoggerAdd x:Name="DatenLoggerAdd" Visibility="{Binding DatenLoggerAddVisible}"></local:DatenLoggerAdd> -->
        <ContentControl Name="ContentMain" Width="Auto" Opacity="1" Background="Transparent" Content="{Binding ContentMain}">
        </ContentControl>
    </Grid>
</UserControl>